version: 2

models:
  - name: fact_destination
    description: "Fact table popular destination by end station."
    columns:
      - name: end_station_name
        description: "The primary key for this table"
        tests:
          - unique
          - not_null